package pro.sky.course2.ExaminerService.repository;

import org.springframework.stereotype.Repository;
import pro.sky.course2.ExaminerService.domain.Question;

@Repository
public class JavaQuestionRepository extends QuestionRepository {
    public JavaQuestionRepository() {
        super();
        getFill();
    }

    public void getFill() {
        add(new Question("Что такое аннотация @Override в Java?", "Аннотация @Override указывает, что метод переопределяет метод суперкласса. Она помогает избежать ошибок при опечатках в имени метода и является обязательной для корректного переопределения."));
        add(new Question("Как работает многопоточность в Java?", "Многопоточность реализуется через класс Thread или интерфейс Runnable. Потоки могут выполняться параллельно, взаимодействовать через синхронизацию и механизмы межпоточного взаимодействия."));
        add(new Question("Что такое generics в Java?", "Generics (обобщения) позволяют создавать классы, интерфейсы и методы, которые работают с различными типами данных без потери типобезопасности."));
        add(new Question("В чём разница между HashMap и HashTable?", "HashMap — не синхронизирован, допускает null ключи и значения, более производительный\n" +
                "\n" +
                "HashTable — синхронизирован, не допускает null, менее производительный"));
        add(new Question("Что такое интерфейс в Java?", "Интерфейс — это абстрактный тип, который содержит только сигнатуры методов и константы. Класс может реализовывать один или несколько интерфейсов."));
        add(new Question("Как работает сборщик мусора в Java?", "Сборщик мусора автоматически освобождает память от объектов, на которые больше нет ссылок. Он использует алгоритм маркировки и очистки (mark-and-sweep) и работает в фоновом режиме."));
        add(new Question("Что такое полиморфизм в Java?", "Полиморфизм — это способность объекта обрабатывать данные разных типов через один и тот же интерфейс. Реализуется через переопределение методов и перегрузку методов."));
        add(new Question("Что такое final, finally и finalize в Java?", "final — модификатор для переменных (значение нельзя изменить), методов (нельзя переопределить) и классов (нельзя наследовать)\n" +
                "\n" +
                "finally — блок кода, который выполняется всегда после try-catch\n" +
                "\n" +
                "finalize — метод, вызываемый перед удалением объекта сборщиком мусора"));
        add(new Question("В чём разница между ArrayList и LinkedList?", "ArrayList — это реализация списка на основе массива, обеспечивает быстрый доступ по индексу (O(1)), но медленные операции вставки/удаления (O(n)). LinkedList — это реализация на основе двусвязного списка, обеспечивает быстрые операции вставки/удаления (O(1)), но медленный доступ по индексу (O(n))."));
        add(new Question("Что такое Java Virtual Machine (JVM)?", "JVM — это виртуальная машина Java, которая позволяет выполнять Java-программы независимо от операционной системы. Она преобразует байт-код в машинный код конкретной платформы."));
    }

}
